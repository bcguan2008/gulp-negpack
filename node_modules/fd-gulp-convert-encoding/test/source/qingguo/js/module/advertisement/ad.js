/**
 * 底部热门推荐
 * @author dongming.jidm
 * date:2012-12-17
 */
!(function($){

    var Sandbox,
        configs = {
            "topAd": "#top_ad",
            end:0
        },self;

    function TopAd(sb) {
        Sandbox = sb;
        return TopAd;
    }

    $.extend(TopAd,{

        init:function(){

            this.config = $.extend(true, {}, configs);

            self = this;

            var ad = $(self.config.topAd);

            if( !ad[0] ) {
                return;
            }

            ad.show();

            self._adaptAd();
        },

        _adaptAd: function() {
            $('#header').css('top','125px');
            $('#content').css('padding-top','240px');
        },

        end:0
    });

    Qingguo.Business.TopAd = TopAd;

    AppCore.register("qingguo_mod_topad", Qingguo.Business.TopAd);

})(jQuery);