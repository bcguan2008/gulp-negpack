// px to rem
exports.rem = function(value) {
    return value / 2 / 16 + 'rem';
};

// getImgUrl
exports.imgurl = function(module, name) {
    return '../../../lib/' + module + '/imgs/' + name + '.png';
};
